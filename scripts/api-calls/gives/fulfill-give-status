#!/bin/sh

cd $(dirname $0)
base_url=$(../utility/base_url)

if [ $# -ge 1 ] ; then
    curl \
      --include \
      --request POST \
      --header "Content-Type: application/json" \
      --data  '{"id": "'$1'"}' \
      $base_url/give/status
else
    echo "Usage: ./fulfill-give-status id"
fi
